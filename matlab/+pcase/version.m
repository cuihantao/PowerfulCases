function v = version()
%VERSION Return the version of PowerfulCases MATLAB API
%   v = pcase.version() returns the version string.
%
%   Example:
%       v = pcase.version();
%       disp(v)  % "0.4.0"

    v = '0.4.0';
end
